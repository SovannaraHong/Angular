import { DirectiveDT } from './directive-dt';

describe('DirectiveDT', () => {
  it('should create an instance', () => {
    const directive = new DirectiveDT();
    expect(directive).toBeTruthy();
  });
});
